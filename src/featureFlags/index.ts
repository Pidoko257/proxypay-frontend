/**
 * Feature Flags module — public surface
 *
 * Import everything feature-flag related from this single entry point:
 *
 *   import { FeatureFlagProvider, useFeatureFlag, FeatureFlagService } from '@site/src/featureFlags';
 */

export * from './feature-flag-service';
export * from './flag-analytics';
export * from './FeatureFlagContext';
